== README

This is a simple messenger app. 

It can take a user-defined number, and a user-defined message, and send the message to the number.

In the current implementation, the phone number needs to be verified by ME, as I have a trial account for twilio.

<tt>rake doc:app</tt>.
